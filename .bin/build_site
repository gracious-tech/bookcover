#!/usr/bin/env bash
# Build the public site (site/dist/). Nothing else needs building first — the site only
# embeds the widget in an iframe, it doesn't import any of the packages.
set -euo pipefail

cd "$(dirname "$0")/../site"

npx vite build
